# Activated with SPRING_PROFILES_ACTIVE=docker (already set in docker-compose.yml).
# Only the datasource host changes: inside the Docker network the Postgres
# container is reachable by its service name ("db"), not "localhost".
spring.datasource.url=${DB_URL:jdbc:postgresql://db:5432/supplychain_ai}
